<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.callum.first.util.Contact">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Click here to call "
        android:id="@+id/Call_button"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/Text_button" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Click here to text"
        android:id="@+id/Text_button"
        android:layout_below="@+id/Email_button"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Click here to email"
        android:id="@+id/Email_button"
        android:layout_below="@+id/Call_button"
        android:layout_alignStart="@+id/Call_button" />
</RelativeLayout>
